# GalbotBimanualRelative 模块化改造项目清单（0525）

## 目标

将 GalbotBimanualRelative 训练 pipeline 改造为**模块化架构**：
- lerobot 数据层只存 abs 原始值，不做任何衍生计算
- 所有 action 变换（delta/chunk_relative）在训练时由自定义 transform 完成
- 所有 stats 离线计算，不与框架自动 stats 计算耦合
- 新增而不破坏原有架构

## 数据层设计（lerobot parquet 列）

| 列名 | dim | 含义 | 用途 |
|---|---|---|---|
| `action` | 20 | t+1 双臂绝对 eef + gripper | 所有变换的原始输入 |
| `observation.state` | 20 | t 双臂绝对 eef + gripper | delta 模式 t=0 时的 base |
| `observation.dual_relative_pose` | 9 | t 左臂在右臂坐标系下 | 输入模型的 state |
| `observation.images.left/right_wrist` | video | | |

**原则**：只存 abs，所有衍生值训练时在线计算。

## 执行清单

### Phase 1: 数据层重构

- [ ] **1.1** 修改 `convert_galbot_bimanual_relative.py`
  - 新增 `observation.state` 列（20d，t 时刻 eef，计算方式同 action 但不用 shift）
  - 将 `observation.bimanual_relative` 改名为 `observation.dual_relative_pose`
  - 更新 `build_features()`
  - 更新 `modality.json` 模板
  - 更新脚本文档

- [ ] **1.2** Smoke test 验证
  - 跑 2 episode
  - 验证各列 shape 和值正确

### Phase 2: 自定义 Transform

- [ ] **2.1** 新增 `starVLA/dataloader/gr00t_lerobot/transform/self_mode_action.py`
  - `SelfModeActionTransform` 类
  - 支持 `self_mode="abs"`：不变
  - 支持 `self_mode="delta"`：逐帧差分，t=0 用 `state[key][0]` 做 base
  - 支持 `self_mode="chunk_relative"`：chunk 内以第 0 帧为 base
  - 注册到 `transform/__init__.py`

### Phase 3: Data Config

- [ ] **3.1** 新增 `GalbotBimanualSelfDataConfig`
  - 新的 state_keys：包含 `dual_relative_pose_*` + `eef_*`（delta base）
  - 新的 action_keys：读 `action` 列
  - `transform()` 流程：`StateActionToTensor → SelfModeActionTransform → StateActionTransform`
  - 注册 `galbot_bimanual_self` 到 `ROBOT_TYPE_CONFIG_MAP`

### Phase 4: 离线 Stats

- [ ] **4.1** 新增 `compute_galbot_stats_self_mode.py`
  - 读所有 parquet 的 abs action + abs state
  - 模拟 chunk 采样（每帧起点，取 chunk_size 步）
  - 应用与 SelfModeActionTransform 相同的变换逻辑
  - gripper 二值化
  - 输出 `stats.json`，`__cache_config: {mode: "abs"}`

### Phase 5: 训练配置

- [ ] **5.1** 更新 modality.json（含 eef 字段）
- [ ] **5.2** 新增 `starvla_qwengroot_galbot_relative_self.yaml`
  - 新增 `self_mode` 参数
  - `action_mode: abs`（框架层面不干涉）
- [ ] **5.3** 新增 `run_galbot_bimanual_self.sh`

### Phase 6: 验证

- [ ] **6.1** Dataloader smoke test（三种 self_mode 各一次）
- [ ] **6.2** 模型 forward smoke test
- [ ] **6.3** 单卡 10 步 debug 训练
- [ ] **6.4** 正式多卡训练

## 文件清单

```
新增：
  starVLA/dataloader/gr00t_lerobot/transform/self_mode_action.py    # Phase 2
  starVLA/examples/GalbotBimanualRelative/train_files/compute_galbot_stats_self_mode.py  # Phase 4
  starVLA/examples/GalbotBimanualRelative/train_files/starvla_qwengroot_galbot_relative_self.yaml  # Phase 5
  starVLA/examples/GalbotBimanualRelative/train_files/run_galbot_bimanual_self.sh  # Phase 5

修改：
  UMI/convert_galbot_bimanual_relative.py    # Phase 1
  starVLA/.../data_config.py                 # Phase 3

不修改（框架核心不动）：
  datasets.py, state_action.py, schema.py, base.py
```